{% extends 'admin/_part/_main.html.twig' %}

{% block title %}Hello AdminJokesController!{% endblock %}

{% block adminmain %}

                JOKE Reports:

    {% include '_part/_jokecard.html.twig' %}

{#    {{ form_start(jokeForm) }}#}
{#    <div class="row">#}
{#        <div class="col-12">#}
{#            {{ form_row(jokeForm.joke) }}#}
{#        </div>#}
{#        <div class="col-4">#}
{#            {{ form_row(jokeForm.created_at) }}#}
{#        </div>#}
{#        <div class="col-4">#}
{#            {{ form_row(jokeForm.updated_at) }}#}
{#        </div>#}
{#        <div class="col-4">#}
{#            {{ form_row(jokeForm.ratingScore) }}#}
{#        </div>#}
{#        {{ form_row(jokeForm.validated) }}#}
{#    </div>#}
{#    {{ form_row(jokeForm.Submit) }}#}
{#    {{ form_end(jokeForm) }}#}

    <table class="table table-dark table-hover w-100 mt-4">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Reason</th>
            <th scope="col">IP</th>
            <th scope="col">Created</th>
            <th scope="col">Action</th>
        </tr>
        </thead>
        <tbody>
        {% for report in joke.reports %}
            <tr>
                <th scope="row">{{ report.id }}</th>
                <td>{{ report.reason }}</td>
                <td>{{ report.ip }}</td>
                <td>{{ report.created_at|date("m/d/Y") }}</td>
                <td><a class="btn btn-sm btn-warning" href="#">Edit</a> <a class="btn btn-sm btn-danger ms-2" href="{{ path('admin.report.del', {id: report.id}) }}">Del</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>



{% endblock %}